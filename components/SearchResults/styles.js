import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  logoContainer: {
    width: '100%',
  },
  logoHeader: {
    fontSize: 20,
    // margin: 8,
    paddingTop: 8,
    color: 'black',
    marginBottom: 12,
    fontWeight: '600',
    width: '100%',
    textAlign: 'center'
  },
});

export default styles;
